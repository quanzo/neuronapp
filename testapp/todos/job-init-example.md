---
tools: intermediate_save
pure_context: false
---

1. Инициализируй состояние демонстрационной задачи оркестратора.

2. Сохрани в промежуточное хранилище:
   - `counter = 0` (описание: `Текущий номер шага цикла`)
   - `target = 3` (описание: `Количество шагов до завершения`)
   - `completed = 0` (описание: `Флаг завершения`)

3. Верни короткий итог:
   - что инициализация выполнена;
   - какие значения сохранены (`counter`, `target`, `completed`).

