---
agent: agent-main
pure_context: false
tools: todo_goto, todo_completed, var_pad
skills: skill-file-block-summarize
---

1. Прочитай переменную `startLine` и если она не установлена, то она равна 0.

2. Загрузи фрагмент файла `"temp/ispytanie.txt"` начиная со строки `startLine` и составь краткое изложение содержимого.

3. Краткие данные по фрагментам файла - далее нам надо будет собрать их вместе.

4. Увеличь номер загруженного чанка на единицу.

5. По результатам загрузки определи последнюю загруженную строку, сохрани ее в переменную `startLine` и увеличь на 1.

6. Файл прочитан полностью только тогда, когда `startLine >= totalLines`. Если файл полностью прочитан, то установи `completed = 1`.

7. Если `completed` не равно 1, то **ПЕРЕЙДИ К ПУНКТУ №2** используя инструмент `todo_goto`

8. Если `completed` больше 0, то значит файл прочитан полностью. Выведи суммарную информацию.
