---
tools: var_set, chunk_size, var_get
pure_context: false
agent: agent-orch
---

1. **Определи путь к файлу**  
   Путь: `temp/671.md`

2. Определи размер файла используя `chunk_size`. Сохрани данные по размеру файла в хранилище.

3. Начальная позиция чтения чанка `start_line = 0`. Номер чанка `part = 0`

4. Сохрани в хранилище:
   - `completed = 0` (описание: `Флаг завершения`)
   - в `path` сохрани путь к файлу
   - `start_line` и `part`

5. Верни короткий итог:
   - что инициализация выполнена;
   - где найден файл
   - какие значения сохранены.
