---
agent: agent-file
pure_context: false
---

1. **Определи путь к файлу**  
   Путь: `temp/671.md`

2. **Получи общее количество строк (один раз)**  
   Если в промежуточном хранилище ещё нет ключа `total_lines`, вызови `size_chunk` для определения `totalLines` и сохрани это значение по ключу `total_lines`.  
   Если `total_lines` уже есть, используй его.

3. Начальная позиция чтения чанка `start_line = 0`. Номер чанка `part = 0`

4. **Прочитай очередной чанк**  
   Вызови `view_chunk` с параметрами:
   - `path`: `temp/671.md`
   - `start_line`: текущее значение `start_line`
   - `lines`: 100
   - `max_chars`: 10000  
   Из результата возьми `startLine`, `endLine`, `chunk`.

5. **Составь краткое резюме**  
   На основе `chunk` составь 1–3 предложения, описывающие основное содержание.  
   Сохрани резюме в промежуточное хранилище с ключом `summary_part_N`, где N — номер чанка.

6. **Обнови прогресс**  
   - Установи новый `start_line = endLine + 1`.
   - Установи новый `part = part + 1`

7. **Проверь завершение**  
   Если `start_line >= total_lines`, установи ключ `completed = 1` иначе `completed = 0`

8. Выведи свой статус

9. Если completed == 0 то перейди к пункту списка 4.

10. Выведи результат работы
