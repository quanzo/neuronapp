---
description: Поиск информации в файле по строке
params: {"query":{"type":"string","description":"Строка, которую надо найти в файле","required":true},"path":{"type":"string","description":"Путь к файлу, в котором проводим поиск","required":true}}
tools: chunk_grep
pure_context: true
---

Твоя задача, используя `chunk_grep` найти в файле все блоки/чанки с интересующим контентом.

Входные данные:
- Запрос на поиск: `query` = "$query"
- Файл в котором ищем: `path` = "$path"


На основании `query` сформируй массив строк для релевантного поиска чанков в файле. `query` может содержать слова, которые не несут смысла и поэтому из поиска их убери. При составлении разных вариантов фраз для поиска можно применять синонимы. Вариантов должно быть не менее 3.

Используй `chunk_grep` и передай массив строк для поиска чанков и путь к файлу.

Вызови инструмент `chunk_grep` и найди блоки с релевантным запросу контентом.

Обработай поступившие данные и подготовь детальный результат работы. Выводы делать не надо. Лишние предлпжение, которые не имеют отношения к задаче - не выводи. Чанки не дублируй и выводи полностью в результат.